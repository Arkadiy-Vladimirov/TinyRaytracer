#include "graphicObjects.hpp"
#include "Image.h"

/*Camera::Camera(Vec3 foc_pos, Vec3 v_dir, double degrees, Vec2 resolution, int a_channels, Vec2 mat_size) 
                                                                : matrix(resolution.x, resolution.y, a_channels){
    focus = foc_pos;
    view_direction = v_dir;
    fov = degrees;
    matrix_size = mat_size;

    //calculate matrix_origin, pixel_size, x_base and y_base.
};*/
